export interface ReservationFilters {
    tableId?: string;
    customerPhone?: string;
    statusId?: string;
    fromDate?: string;
    toDate?: string;
    date?: string;   // // دقیقاً یک رورز
    time?:string
}
